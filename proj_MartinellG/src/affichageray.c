// Affichage Raylib :
	/*
	const int largeur = 800; //(pixels)
	const int hauteur = 450;
	InitWindow(largeur, hauteur,"raylib basic window");
	while (!WindowShouldClose()){
		BeginDrawing();
		ClearBackground(RAYWHITE);
		DrawText("Hello world !", 10, 20, 20, BLACK);
		DrawPixel(100, 100, BLACK);
		EndDrawing();
		
	}
	*/

// Coolors site avec des palettes de couleurs stylées
